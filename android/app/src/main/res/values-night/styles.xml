<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!--
    هذا الملف هو نسخة الثيم الليلي (Night Mode)
    يجب أن يكون متطابقًا في نوع الثيم مع values/styles.xml
    ويعتمد Material Components فقط
    -->

    <!-- Flutter launch theme (Night) -->
    <style name="LaunchTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:windowBackground">@drawable/launch_background</item>
    </style>

    <!-- Flutter normal theme (Night) -->
    <style name="NormalTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:windowBackground">?android:colorBackground</item>
    </style>

    <!-- UCrop theme (Night) -->
    <style name="Theme.UCrop" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">#000000</item>
        <item name="colorPrimaryVariant">#000000</item>
        <item name="colorSecondary">#00FFAA</item>
    </style>

</resources>
